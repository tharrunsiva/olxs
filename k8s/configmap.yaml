apiVersion: v1
kind: ConfigMap
metadata:
  name: reloop-config
  labels:
    app.kubernetes.io/name: reloop
    app.kubernetes.io/part-of: reloop-app
data:
  PORT: "5000"
  NODE_ENV: "production"
  MONGO_URI: "mongodb://mongo-service:27017/reloop"
